
const initailState = {
    isAuth: null
}

const loginReducer = (state = initailState, action)=>{
    switch(action.type){
        default:
            return state
    }
}

export default loginReducer